import { RandomChoice } from "../interfaces/randomChoice";
export declare const weighted_random: (items: any[], weights: number[]) => RandomChoice;
export declare const selectRandomFromArray: (array: string[]) => string;
